npm init -y --> env setup
install express --> npm i express

An API is a bridge that allows one software/app to talk to another software/app.

installed nodemon (npm i nodemon) and edited the 'script' in 'package.json' to ensure auto restrat